# Changelog

## [2.3.270] - 2026-08-04

### 修复

- 修复 Agent/FXP 升级或面板配置变化后复用旧进程，导致 ForwardX 隧道可转发但流量不上报。
- 修复 GOST 等进程转发短连接漏计，导致 24H 连接数长期为 `0`。
- 增加 FXP 流量上报限频诊断，并保证失败批次幂等重试。
- 修复 GOST/ForwardX UDP 会话提前关闭及重建后断流，降低高包速率开销并限制队列与分片内存。
- Agent 心跳失败采用单一有限重试队列，避免请求叠加，并在任意心跳成功后立即取消重试。
- 修复 Agent Token 删除时待清理规则误拦截，并在真实规则占用时显示所属用户。

### 版本

- 面板与 APK Release `2.3.270`，Agent `2.2.184`，ForwardX FXP runtime `2.2.112`，Android APP `2.3.97`。

## [2.3.269] - 2026-08-02

### 修复

- 修复普通用户转发组流量与连接数归属，以及 Agent/FXP 连接累计不准确。
- 修复可选访问限制和 TCP/UDP 计数链恢复导致的重复重启或计数中断。
- 修复批量导入、复制及跨组迁移的入口端口误判和并发分配冲突。
- 修复隧道转发延迟分段误显示“等待探测”和出口组文案错误。

### 版本

- 面板与 APK Release `2.3.269`，Agent `2.2.183`，ForwardX FXP runtime `2.2.111`，Android APP `2.3.97`。

## [2.3.268] - 2026-08-01

### 新增

- 增加套餐流量与按量计费流量分开统计及重置展示基线。
- 增加自定义菜单的面板路径、内嵌和新窗口打开方式。
- 补充隧道、多入口延迟分支详情和故障排查文档截图。

### 修复

- 修复 Agent/FXP 在重启、升级及 V1/V2、WireGuard、GOST 切换后的运行态恢复和监听校验。
- 修复 Agent 流量计数链恢复、规则修复时的重复清理和计数丢失，保留已有防火墙计数。
- 修复隧道、转发链和多入口探测的拓扑匹配、并发刷新、超时及旧结果串用。
- 修复延迟详情节点归属、普通用户流量展示和自定义菜单嵌入兼容性。

### 版本

- 面板与 APK Release `2.3.268`，Agent `2.2.182`，ForwardX FXP runtime `2.2.110`，Android APP `2.3.97`。

## [2.3.267] - 2026-08-01

### 修复

- 修复支付回调重新激活已关闭订单，并启用 PostgreSQL TLS 证书校验。

### 版本

- 面板与 APK Release `2.3.267`，Agent `2.2.181`，ForwardX FXP runtime `2.2.110`，Android APP `2.3.97`。

## [2.3.266] - 2026-07-30

### 新增

- 主机累计流量支持“用量修正”，可按实际用量覆盖当前统计值。

### 修复

- 修复 GOST 六种原生传输、单 UDP 规则协议和多跳中继鉴权配置。
- 修复 ForwardX FXP 切换 GOST 时旧运行态残留及认证失败，增加事务交接、失败回滚和运行态恢复。
- 修复普通用户规则列表按账号隔离，以及筛选无结果后无法返回的问题。

### 优化

- 调整主机卡片和列表快捷操作，取消瞬时流量动画，并明确 Agent Token 创建时间展示。

### 版本

- 面板与 APK Release `2.3.266`，Agent `2.2.181`，ForwardX FXP runtime `2.2.110`，Android APP `2.3.97`。

## [2.3.265] - 2026-07-30

### 修复

- 稳定 Agent 在 ForwardX V1/V2、WireGuard 与 GOST/Nginx 间切换时的运行态交接、监听就绪和失败恢复。
- 转发链支持各成员独立使用主机端口区间，并修复同步回滚、链路探测和流量归属。
- Docker 升级增加镜像与运行版本校验，避免旧实例仍在运行时误报升级成功。
- Docker 镜像改为 amd64/arm64 原生构建并验证后合并，避免 QEMU 异常导致 ARM64 镜像缺失。
- 修复隧道延迟刷新超时边界偶发重复查询。

### 版本

- 面板与 APK Release `2.3.265`，Agent `2.2.180`，ForwardX FXP runtime `2.2.110`，Android APP `2.3.96`。

## [2.3.264] - 2026-07-29

### 新增

- Nginx Stream 自定义证书支持拖放或多选上传 PEM 证书链和私钥。

### 修复

- 修复 ForwardX V1/V2 会话、队列、握手及规则替换造成的内存、连接和旧运行态回收问题。
- 修复 Nginx Stream TCP 长连接超时、入口证书处理和旧证书清理问题，补充会话诊断日志。

### 优化

- 优化大批量 ForwardX V1/V2 入口规则合并，降低 Agent 的 CPU 与 GC 压力。
- 优化 Agent Token 备注展示，并统一隧道探测超时文案。

### 验证

- 类型检查、前端构建、Agent/FXP 测试、竞态测试和 `go vet` 通过。

### 版本

- 面板与 APK Release `2.3.264`，Agent `2.2.179`，ForwardX FXP runtime `2.2.110`，Android APP `2.3.96`。

## [2.3.263] - 2026-07-29

### 修复

- 修复 ForwardX FXP 多入口及 V1/V2 混用时的运行态残留、误清理和 WireGuard peer 未就绪问题。
- 稳定转发组健康切换，避免短暂超时或面板通讯波动造成入口、DNS 和规则状态反复切换。
- 修复资源撤权、规则迁移和按量计费授权不一致的问题，并避免旧规则清理误删同端口新规则。
- 拆分端口转发与网络测试主机授权，普通用户不再看到未授权的底层主机。
- 修复域名到期或 DNS 失败引发的重复解析和运行态抖动，失败时保留最后有效地址。
- 修复 iptables、nftables、Realm、Socat、GOST、Nginx 与 ForwardX 的流量重复、串流或漏计问题。
- 减少大规则量下防火墙计数规则的扫描和重建，降低域名变化、Agent 重启时的 CPU 峰值，并兼容 Alpine/BusyBox。

### 验证

- 服务端 `427/427`、Agent/FXP 测试、类型检查、生产与文档构建均通过；未执行 Docker 构建。

### 版本

- 面板与 APK Release `2.3.263`，Agent `2.2.178`，ForwardX FXP runtime `2.2.109`，Android APP `2.3.96`。

## [2.3.262] - 2026-07-28

### 修复

- 修复 Agent 注册、心跳、事件流与 ForwardX FXP 流量上报在主控和被控系统时间不一致时反复返回 `Request timestamp out of window`、导致安装注册失败或通讯不稳定的问题；新版 Agent/FXP 使用面板签发、经 HMAC 保护且只能消费一次的 `challenge-v2`，加密通讯不再依赖双方系统时间。
- 修复面板重启或挑战批次失效时，FXP 并发请求中后返回的旧挑战 401 可能不重试、造成单次流量或协议阻断上报遗漏的问题；明确收到 v2 拒绝时每个请求最多重试一次，并复用原加密批次和幂等 ID，避免重复统计或重复计费。
- 修复已从 Nginx Stream 切换到 V1/FXP 后，Agent 仍可能保留旧 Nginx systemd 服务、进程、配置、PID、证书和日志，导致两台主机规律性反复同步旧运行态的问题；当前拓扑不再使用 Nginx 时会完整退役所有受管状态。
- 修复 Docker 卸载后重新安装、新设管理员账号密码后仍提示错误的问题；重装检测到旧 data volume 时明确保留 `/data/database.json`、SQLite 数据与原管理员，不再用新的数据库选择覆盖旧配置。
- 修复 Docker 卸载只按默认项目名删除 volume、非标准实际挂载删除失败后重试会遗漏旧数据并误报成功的问题；卸载现在从容器解析 `/data` 实际卷名并持久化待删列表，删除失败时保留部署目录并返回错误；外部 MySQL/PostgreSQL 数据不会被 Docker 卸载清空。
- 修复首次初始化中已有管理员仍可进入创建流程、活动迁移导入管理员后提前跳转登录页、多个初始化写操作竞态以及管理员与设置部分提交的问题；初始化写入统一串行化，关键数据使用事务提交。
- 修复初始化标记存在但数据库无管理员时可从公网抢建账号、同时泄露数据库配置、错误和业务表行数的问题；此状态保持安全锁并只显示恢复指引。
- 修复管理员邮箱大小写不一致时登录失败、页面提示支持邮箱但服务端只查用户名、Safari 初始化表单自动填充不稳定，以及密码首尾字符被意外删除或纯空格密码无法登录的问题；邮箱统一小写保存并支持唯一、不区分大小写的用户名或邮箱查询，表单增加确认密码与 Safari 友好字段。

### 优化

- 为仍使用 v1 时间窗口的旧版 Agent/FXP 增加面板协议时钟自动校准；通过多个可配置 HTTPS `Date` 来源交叉验证，仅校准协议时间基准，不修改宿主机或 Docker 系统时间；保留 v1 防重放兼容，不降低旧版通讯的验证强度。
- 移除无效的 `ADMIN_PASSWORD` 环境变量示例，补充 Docker 数据卷复用、完整卸载、外部数据库与多实例挑战粘性路由说明。

### 验证

- 通过服务端全量测试（411 项）、TypeScript 类型检查、生产构建、文档构建、Agent/FXP Go 测试与 `go vet`、Docker 安装脚本语法检查、版本一致性检查和 `git diff --check`；回归覆盖 iptables/nftables 原始入口端口计数、FXP 幂等流量上报、挑战并发失效、初始化竞态与 Docker 实际数据卷卸载。

### 版本

- 面板版本与 APK Release 版本升级至 `2.3.262`。
- Agent 版本升级至 `2.2.177`；ForwardX FXP runtime 日志版本升级至 `2.2.108`；Android APP 版本保持 `2.3.96`。

## [2.3.261] - 2026-07-27

### 修复

- 补齐本版提交中遗漏的手动探测结果交接与规则探测稳定缓存模块 `manualTestCache`、`ruleProbeCache`；规则和转发组手动探测完成时会先把本次结果写入非活动查询缓存，再一次性退出“探测中”，避免切换查询键后短暂回显上一次结果或因模块缺失导致前端构建失败。
- 修复隧道手动探测在一次轮询间隔内快速完成、且秒级 `lastTestAt` 与上次相同时可能一直停留在“探测中”、需要刷新页面才能看到结果的问题；发起探测后强制刷新当前隧道详情，并以 mutation 后的新查询快照确认完成，等待期间继续保持探测状态，不闪回旧结果。
- 修复同一秒内连续产生多条规则或转发组探测记录时，单凭创建、更新时间无法稳定选中最新结果的问题；活动记录和已完成记录在时间相同时统一按 `id DESC` 选择最新一条。
- 修复主机探测服务延迟图表的时间范围下拉仍固定查询 24 小时、服务端按正序截取后拿到最旧数据，以及前端以最后一个旧点作为筛选锚点的问题；现在按所选范围查询，以当前时间筛选，并先取得数据库中最新的限制条数后恢复为时间正序。
- 修复规则页面切换、筛选或轮询空档时最近探测数据短暂消失，导致运行中的规则同时由绿色变黄、稍后又恢复的问题；最近有效探测按用户稳定缓存，规则修改、启停、删除和批量操作时主动失效，普通规则与转发组规则统一使用明确的状态优先级。
- 修复 V2 WireGuard 多入口在 runtime 尚未就绪或探测期间发生 runtime 重建时误报超时的问题；Agent 会丢弃旧 runtime 代际的规则、隧道和转发组探测结果，WG runtime/peer 未就绪时不再生成自动超时。
- 修复依赖 runtime 的隧道与多入口探测沿用 8 秒普通探测预算、启动较慢时稳定超时的问题；Agent runtime 就绪等待恢复为 20 秒，动作完成后等待 5 秒，服务端仅对此类探测使用 30 秒预算，普通探测仍为 8 秒。
- 修复转发组健康判断中旧的进程内认证时间遮蔽数据库内更新心跳、多实例或请求路径切换时可能短暂误判 Agent 离线的问题；认证活动与持久化心跳改为取较新时间，只有仍在有效期内的认证活动可以覆盖旧的 `isOnline` 标志。
- 修复入口组一次国内健康度超时就立即剔除、下一次成功又立即恢复并连续推送两条切换通知的问题；入口 Agent 活性和国内健康度现在统一经过 `failoverSeconds` 与 `recoverSeconds` 观察窗口，未知健康状态不会掩盖 Agent 离线，健康快照在 DDNS 提交前变化时会放弃旧选择并重新评估。
- 修复 Mimic fake-TCP 数据可能被 GRO/LRO 合并后交给 userspace WireGuard、造成丢包和延迟尖峰的问题；Mimic 活跃时仅关闭可变的 `GRO`、`LRO` 和 `rx-gro-hw`，保留 checksum、GSO 与 TSO，服务停止或异常退出后恢复原状态，并按网卡串行化调优与恢复。

### 优化

- WireGuard 代理与 FXP UDP 队列拥塞时改为淘汰最旧数据报；超过 25ms 的包仅在存在更新包时丢弃，保留孤立的 DNS、OpenVPN、QUIC 等 UDP 数据报，WG 积压写入同时受剩余时效约束，减少短时阻塞后回放旧包造成的游戏延迟尖峰。
- Mimic 网卡调优失败改为 30 秒后重试，网卡重建时自动使缓存失效，并兼容迁移旧 Agent 保存的 offload 快照；Mimic WG MTU 调整为 `1280`，生成配置启用 `max_window = true`。

### 验证

- 新增手动探测结果交接、mutation 后查询快照、同时间戳最新探测记录、规则探测稳定缓存、状态优先级、时间范围筛选、最新服务延迟查询、WG runtime 代际、runtime 未就绪、UDP 队列时效、Mimic offload 并发恢复、入口健康窗口与心跳时间选择等回归覆盖，并调整 FXP 中继测试端口分配时机以避免端口复用干扰。
- 通过服务端全量测试（382 项）与 Agent 加密专项测试（3 项）、TypeScript 类型检查、生产构建、文档构建、Agent/FXP Go 测试和 `go vet`、Linux `amd64/arm64` 发布资产构建、版本/tag 一致性检查及 `git diff --check`。

### 版本

- 面板版本与 APK Release 版本升级至 `2.3.261`。
- Agent 版本升级至 `2.2.176`，ForwardX FXP runtime 日志版本升级至 `2.2.107`，Android APP 版本保持 `2.3.96`。

## [2.3.260] - 2026-07-27

### 修复

- 修复 Safari 导出加密备份到 100% 后因下载链接和 Blob URL 被立即清理而提示 `The string did not match the expected pattern.` 的问题；下载资源改为延迟清理，Blob 下载被浏览器拒绝时仅对小文件回退兼容下载，避免大备份转为 data URL 时额外占满 CPU 和内存。
- 区分服务端备份生成失败、备份已生成与浏览器保存失败；Safari 未接管下载时保留当前页面中已生成的加密备份并提供重新保存入口，无需再次读取数据和加密，重新保存增加短暂防重复保护，日志与支持包下载同步复用兼容逻辑。
- 修复规则、转发链和隧道的延迟探测弹窗首次打开、用户尚未点击测试时误显示“探测中”的问题；现在优先展示上一次已完成的上报结果，没有历史结果时显示“等待探测”，只有本次手动发起后才进入探测中状态；转发组历史结果按完整组 ID 精确匹配，避免组 `1` 误读组 `10/11` 的记录。
- 修复转发规则在入口主机同时具备 IPv4 或域名与 IPv6 时重复展示 IPv6 入口的问题；规则卡片、精简卡片和列表统一优先展示 IPv4 或域名，仅在没有两者时展示 IPv6，不改变实际监听、路由和入口搜索。
- 修复 Mimic 关闭后快速重新开启时，相同过滤配置可能沿用旧同步状态、出现服务进程正常但 UDP 数据通道不通的问题；即使关闭和重新开启发生在两次心跳之间，也会根据规则/隧道启停修订生成新的生命周期代际，强制清理残留 XDP/TC hook 与锁文件、重载内核模块并等待 hook 就绪，同一健康拓扑不会重复重启。
- 修复较旧的 Mimic 关闭或同步动作在等待串行锁后仍可能晚于新开启动作执行的问题；Agent 取得锁后再次校验动作时效并丢弃旧动作，同时将 shell、systemd 与 Mimic 的实际失败摘要回传面板，运行计划日志补充 `active`、`hooks` 和连接状态，不再把 `reported=yes` 误解为数据通道健康。
- 修复主机、Agent Token、主机探测服务、主机分组、转发组、转发规则和隧道等服务端排序列表在拖动结束后先回到旧顺序、等待刷新后才显示新顺序的问题；松手后立即保留新排序，后台保存与重新同步期间不再切换列表渲染树或隐藏拖动手柄，失败时以服务端数据恢复。
- 分页占位数据、搜索结果、筛选子集、全部规则分类或跨用户范围等不完整排序作用域下不再允许提交拖动顺序，并在保存期间阻止重复拖动，避免旧页或局部 ID 覆盖全局 `sortOrder`。
- 稳定主机和规则流量统计请求中的 ID 顺序；仅改变列表显示顺序时不再生成新的统计查询键，避免流量数据发生无意义的重新请求或短暂闪烁。
- 修复 iptables 与 nftables 内核转发在 `FORWARD` hook 中按共享目标串流或重复累计的问题；入站、回程和最终放行规则统一绑定 conntrack 原始入口端口，同时保留 `PREROUTING`/`POSTROUTING` 与 `FORWARD` 计数回退，避免再次出现 iptables/nftables 有转发但统计为零。
- 修复 Realm、Socat、GOST 与 Nginx 等进程转发在多个监听端口共用目标时按目标地址串流的问题；计数改为以各自监听端口的入站/出站 hook 为准，IPv4、IPv6、TCP、UDP 及 nft 进程计数均保留覆盖。
- 修复 ForwardX FXP 自报流量与 Agent 防火墙计数重复累计，以及入口组、负载均衡额外出口上报被错误忽略的问题；GOST/Nginx 隧道只接受当前合法出口，ForwardX 隧道只接受合法入口，避免多跳链路重复计费。
- 修复 Agent 或 FXP 上报成功但响应丢失时重复统计、重复扣费，以及并发重试先收到“重复成功”后原事务回滚导致永久漏计的问题；每批使用跨进程唯一 ID，Agent 先持久化固定快照，幂等 claim 与流量、配额和计费在同一事务提交，失败继续以同一批次重试。
- 修复流量计费实时上报与规则/转发组删除结算的锁顺序不一致，可能死锁或留下未结算尾量的问题；关键出口拓扑、累计计数或聚合桶写入失败时整批回滚，不再吞错后确认成功。
- 修复部分流量聚合桶遮蔽 raw 明细、bucket 查询失败或返回空数组时显示空曲线的问题；升级时重建旧版可能缺失的 30 分钟聚合桶，规则汇总以保留期内 raw 数据为准，规则与全局曲线在 bucket 缺失、为空或不可用时回退 raw。
- 修复 SQLite 单连接在异步事务期间仍可能被事务外 Drizzle 或原始 SQL 查询插入执行、破坏事务隔离的问题；所有查询、事务与独占操作统一使用可重入 FIFO 连接锁。
- 精简迁移和面板备份保留 Agent 流量幂等状态，避免迁移窗口内已入账但响应丢失的批次在新面板重复累计；SQLite 直连迁移仍兼容缺少新幂等表的旧版本备份，导入后由当前 schema 创建空表。

### 优化

- Agent 待发送流量快照绑定面板地址与 token 身份，重新绑定时不会把旧面板批次发给新身份；首次建立身份时清除归属未知的旧基线，损坏或不完整的 pending 文件会停止推进基线并等待人工处理，不会静默漏计。
- Agent 使用跨平台单实例状态锁保护流量快照与基线，原子替换和删除后同步持久化目录元数据；已确认重复批次会刷新保留时间，旧版 reportId-only 幂等记录独立保留至少 7 天。
- 发布工作流校验 tag 与面板版本一致，并校验 Android APK、Agent 和 ForwardX FXP runtime 的独立版本；多架构镜像构建显式写入当前面板版本，避免发布资产版本错配。

### 验证

- 审核全部前端拖动入口；本地多跳编辑器与转发组成员编辑器原本已在拖动后立即更新本地状态，无需修改。
- 新增共享目标、原始入口端口、ForwardX 去重、固定重试快照、幂等事务、聚合桶缺失/空数组/查询失败、SQLite 事务隔离与旧 SQLite 备份等回归测试。
- 新增路由级流量归属回归，覆盖 ForwardX 主入口与入口组成员、GOST/Nginx 主出口与启用的负载均衡额外出口，以及禁用、历史或策略关闭的出口拒绝场景。
- 新增 Mimic 健康代际、关闭重开、同代际稳定、异常转移、锁等待期间旧动作失效及失败输出截取回归测试。
- 新增 Safari Blob 下载延迟清理、小文件 data URL 回退、大文件扩容保护、双路径失败提示与文件名清理回归测试；新增探测弹窗等待、历史结果和手动探测状态回归测试。
- 通过 Agent/FXP Go 测试、服务端全量测试（365 项）、TypeScript 类型检查、生产构建、版本检查及 `git diff --check`。

### 版本

- 面板版本与 APK Release 版本升级至 `2.3.260`。
- Agent 版本升级至 `2.2.175`，ForwardX FXP runtime 日志版本升级至 `2.2.106`，Android APP 版本升级至 `2.3.96`。

## [2.3.259] - 2026-07-26

### 修复

- 修复同一主机的多个 iptables/nftables 内核转发入口指向相同 DNAT 目标时，`FORWARD` hook 计数可能被每条规则同时命中、造成流量重复统计和重复计费的问题；转发计数现在同时匹配 conntrack 原始入口端口，仍保留 `FORWARD` hook 以及原有 `PREROUTING`/`POSTROUTING` 计数，避免重新出现内核转发流量为零。

### 验证

- 新增同目标、不同入口端口的 iptables 与 nftables 计数隔离回归测试，并通过服务端全量测试（341 项）、TypeScript 类型检查、生产构建、版本检查及 `git diff --check`。

### 版本

- 面板版本与 APK Release 版本升级至 `2.3.259`。
- Agent 版本保持 `2.2.174`，Android APP 版本保持 `2.3.95`，ForwardX FXP runtime 日志版本保持 `2.2.105`。

## [2.3.258] - 2026-07-26

### 修复

- 修复内核转发规则在 nftables `input`/`output` hook 中无法统计转发流量、可能将已有计数覆盖为零的问题；新增按 DNAT 目标匹配的 `forward` hook 计数，并按方向合并实际命中的计数结果，兼容 IPv4、IPv6、TCP 和 UDP。
- 修复私网隧道的历史多跳、备用出口和入口组配置在未显式保存连接地址时回退到公网地址的问题；运行计划、链路测试、Mimic 过滤和 ForwardX WireGuard 节点统一优先使用主机的隧道内网地址。
- 修复 Mimic 服务进程处于 active 但 XDP/TC hook 未完整挂载时仍被判定健康的问题；Agent 现在会校验 XDP 与 TC egress hook、清理残留程序和锁文件，并在 `native`/`skb` 模式间自动回退后重新确认运行状态。
- 主机离线判定窗口由 240 秒调整为 150 秒，与 Agent 60 秒空闲心跳周期及网络调度余量匹配，减少主机断联后长时间显示在线的问题。

### 优化

- 仪表盘、规则、主机、隧道、转发组、用户、套餐、计费、支付、商店和钱包页面在后台刷新及分页切换时保留上一份有效数据；统计值、规则分类数量和分页总数不再短暂闪为零或空白，缓存同时按账号与筛选作用域隔离。
- 稳定拖拽排序上下文中的项目引用，避免后台轮询重渲染时相邻卡片跳动；主机分组排序成功后同步刷新列表和地图缓存，创建规则切换路由类型时也不再触发干扰输入的内容过渡。

### 安装与运行

- Mimic 安装器适配上游按 Debian/Ubuntu 发行版代号命名的安装包，并完善 DKMS 与源码回退所需的内核头、BTF、bpftool、kfunc/kprobe 和构建工具检测。
- Mimic 源码回退现在会构建并安装 CLI、内核模块和 systemd 模板，补充 `CAP_BPF` 权限、旧 DKMS 状态清理、模块加载验证及 kfunc 失败后的 kprobe 回退；生成配置同时兼容官方包与未启用 libxdp 的源码构建。

### 验证

- 通过服务端全量测试（340 项）、TypeScript 类型检查、生产与文档构建、Agent/FXP Go 测试与 `go vet`、Mimic 安装脚本语法检查、版本检查及 `git diff --check`。

### 版本

- 面板版本与 APK Release 版本升级至 `2.3.258`。
- Agent 版本升级至 `2.2.174`；Android APP 版本保持 `2.3.95`，ForwardX FXP runtime 日志版本保持 `2.2.105`。

## [2.3.257] - 2026-07-25

### 修复

- 修复普通用户无法创建规则，以及已有转发组显示“入口地址不可用”或“暂无可用端口转发”的问题。访问范围现在会纳入用户自有转发组，并按组展开启用成员、隧道入口/出口、跳板节点和启用的备用出口；组内资源只在所属转发组上下文中可见，不会因此扩大独立主机或隧道权限。
- 修复共享、订阅和按流量计费转发资源的成员、入口地址和隧道端点被错误过滤的问题；兼容数据库返回的字符串、数字和布尔型启用状态，保留按量计费产品授权后的可用资源。
- 修复管理员调整主机、转发组、隧道或按量计费授权后访问范围缓存未立即失效的问题；删除用户时同步清理相关缓存。

### 优化

- 管理员用户权限页新增主机授权的选择、查看和移除；保存流量设置时保留已有主机授权，权限数据仍在加载时禁止保存，避免未加载数据覆盖现有授权。

### 官方插件

- 中国区域白名单插件升级至 `0.7.0`：移除旧的第三方安装、引导、Python 数据处理和防火墙脚本，改用 ForwardX 自有非交互防火墙适配，支持 nftables、iptables/ipset、全局和端口策略、DNAT/FORWARD、接口范围、ASN 更新及 systemd 持久化。
- 白名单规则渲染增加重叠 CIDR 规整、原始 DNAT 端口匹配、iptables 出/入接口处理和 nftables/iptables 后端切换清理；插件清单、官方商店、说明文档和压缩包同步更新，并补充数据来源与许可证边界。

### 安装与发布

- Mimic 安装器改为直接使用上游 Mimic 发布物，不再依赖外部封装脚本；增加 Debian/RHEL 预编译包、DKMS/源码回退、镜像加速和已有 systemd 服务状态恢复。
- Agent 发布脚本、Agent Release 工作流和 Docker 镜像随附 `THIRD_PARTY_NOTICES.md`，README 与插件页面补充许可证声明入口。

### 文档

- 重整并扩充 `docs/guide` 全套使用文档，覆盖部署与初始化、Agent 安装升级、主机和资源组、转发链与隧道、DDNS/故障转移、通知、插件、流量与延迟、PROXY Protocol、常见故障排查、备份迁移和卸载；同步更新快速开始、教程导航及相关配置说明。
- README、白名单插件说明和插件开发文档同步补充当前资源权限、Mimic、数据来源、第三方许可证及发布物说明。

### 验证

- 通过服务端全量测试（331 项）、TypeScript 类型检查、插件清单测试、插件打包、相关 Shell 语法检查及 `git diff --check`。

### 版本

- 面板版本与 APK Release 版本升级至 `2.3.257`。
- Agent 版本升级至 `2.2.173`，Android APP 版本保持 `2.3.95`。
