# ForwardX

ForwardX 通过轻量 Agent 统一管理多台 Linux 主机上的端口转发、加密隧道、转发链和用户计费。本文件只定义领域词汇，不描述实现。

## Language

### 链路资源

链路资源在「链路管理」中创建，业务端口和目标地址在「转发规则」中配置。规则只引用已保存的资源。

**转发规则**:
用户实际访问的业务端口、协议和最终目标，引用一个已保存的链路资源。
_Avoid_: 端口规则、转发条目

**隧道**:
把入口主机和出口主机连成一段链路的资源，支持多跳、入口组、出口组和加密传输。
_Avoid_: 通道、tunnel 链路

**转发链**:
一条固定的多跳路径，成员是有序的主机列表，每一跳可以使用不同的转发方式。它是路径编排层，不是传输协议。
_Avoid_: 端口转发链、中转链、路由链

**入口组**:
可复用的多台入口主机集合，供隧道或转发链引用；成员同时都能接收流量。
_Avoid_: 入口集合、多入口组

**出口组**:
可复用的多台出口主机集合，只能被隧道引用，自带故障转移和负载均衡。
_Avoid_: 落地组

**转发组**:
多台入口主机为同一目标提供高可用的资源，任意时刻只有一台是当前入口，失效后配合 DDNS 切换。与入口组不同：入口组是并行复用，转发组是主备切换。
_Avoid_: 故障转移组、高可用组

### SNI 分流

**SNI 分流规则**:
一条带 SNI 域名的转发规则。同一入口端口上可以并存多条，靠域名互相区分。
_Avoid_: SNI 路由、分流条目、域名规则

**分流器**:
出口主机上的独立运行时角色，读取客户端 TLS 握手中的 SNI 并据此选择落地机。
_Avoid_: 分流节点、SNI 代理、splitter

**分流表**:
分流器在运行时持有的「域名 → 落地机」映射，由面板从 SNI 分流规则推导后下发。它是派生物，不是用户直接创建或管理的资源。
_Avoid_: 路由表、SNI 路由表、域名表

**落地机**:
SNI 分流规则的最终目标主机，分流器按域名把连接交给它。
_Avoid_: 后端、上游、目标机

### 运行时

**期望状态**:
面板为某台 Agent 计算出的完整配置快照，Agent 通过心跳取得后向其收敛。
_Avoid_: 目标状态、配置下发

**受管运行时**:
由 Agent 安装并托管的转发进程，与主机上用户自有的同名服务互不干扰。
受管运行时自身的监听属于受管占用；只有规则确实共用同一个监听时，该占用才可复用。
_Avoid_: 托管进程、内置服务

**端口占用快照**:
Agent 就本主机启用规则所用的入口端口上报占用情况，并逐项声明本次核实过的端口与协议。快照记录绑定地址和可识别的进程；按端口成组裁剪时，被丢弃的端口也退出覆盖声明，采集和请求体仍有明确上限。快照并非用户创建的资源。

**外部占用**:
监听端口的进程未被证实属于 ForwardX 受管运行时，或者监听无法由当前规则复用。

**端口占用警告**:
内核转发规则的入口端口出现监听时，与运行状态并列显示的标记；规则仍可能正常运行。

**forwardType**:
转发规则的执行方式标签。注意它不等于传输协议：所有由隧道承载的规则一律记为 `gost`，真正的传输方式由隧道自身的 `mode` 决定。
_Avoid_: 把 forwardType 当作协议名使用
