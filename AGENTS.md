# 仓库远端约定

1. 所有代码提交、版本标签和发布操作只推送到 `sni-panel` 远端的 `main` 分支，仓库地址为 `https://github.com/owo10086/sni-panel.git`。
2. `origin` 远端对应 `https://github.com/poouo/Forwardx.git`，仅作为上游参考来源，禁止向该远端推送代码、标签或发布内容。
3. 创建新版时，版本标签与 `main` 分支一并推送到 `sni-panel` 远端。
