# 05 — 文档、领域词汇与发布

**What to build:** 把本次改动落到领域词汇表、ADR、旧规格与旧工单、发布说明里，使后来的人不会照着一份已被撤除的规格或一张已不作数的工单重建创建时校验。

**Blocked by:** 01, 02, 03, 04

**Status:** ready-for-agent

## 领域词汇

- [ ] `CONTEXT.md` 的**端口占用快照**词条：定义由「Agent 从主机监听情况采集的派生数据」改为「Agent 就自己规则用到的入口端口上报的占用情况，**并声明本次核实过哪些端口与协议**」；去掉与按重要程度截断、按端口号表达完整性有关的表述
- [ ] 去掉的**只是**那两样。新设计仍有逐项枚举的覆盖集合与两道上限，词条不得把覆盖声明与体积约束一起抹掉——那会让下一个读词条的人以为上限被取消了
- [ ] `CONTEXT.md` 的**端口占用警告**词条：补上「只对内核转发规则呈现」
- [ ] **外部占用**、**受管运行时**词条不变

## ADR

`.scratch/` 是草稿区，本规格本身不是长期文档。这两件事必须落到 `docs/adr/`（已有 0001、0002），否则会随规格一起被遗忘：

- [ ] 一条 ADR 记下**「裁剪的维度与声明的维度必须是同一个」**（User Story 35）。它不是本功能的实现细节，而是一条会在任何「有上限的派生数据」上重演的约束；正文要写清本次是怎么违反它的，以及违反之后为什么不会报错、只会静悄悄地把功能关掉
- [ ] 一条 ADR 记下**创建时的主机端口校验被有意撤除**。它撤掉的是上一版规格的主线，正文要带上重新引入的门槛：**必须先回答它比「先建、冲突了再报错」多提供了什么**

## 旧规格与旧工单

- [ ] 在 `.scratch/sni-port/spec.md` 顶部标注：与端口占用有关的内容已被 `.scratch/port-occupancy-rework/spec.md` 取代，其中创建时的主机端口校验已被整体撤除
- [ ] 不删除旧规格正文——它记录了当初为什么那样设计，以及本次缺陷的来源
- [ ] 动这个文件之前先处理掉它工作区里那 122 行未提交改动，别把标注和一批无关改动混在同一次提交里
- [ ] **上一版规格的 9 张工单（`.scratch/sni-port/issues/01..09`）也要标记作废**，其中 03、05、07、08、09 正是本规格撤除的创建校验。只在旧 spec 顶部加一句取代说明，挡不住一个直接拿工单去做的人
- [ ] 这 9 个文件目前连 `Status:` 行都没有（不符合 `docs/agents/issue-tracker.md`），标注作废时按 `docs/agents/triage-labels.md` 一并补上 `Status: wontfix`，并在标题下写明被哪份规格取代

## 发布

- [ ] 面板与 Agent 一起推小版本位，ForwardX FXP runtime 跟随 Agent，按仓库既有版本规则执行并通过版本校验脚本
- [ ] CHANGELOG 说明两件事：端口占用检测改为只核实规则用到的端口（修复无关监听过多的主机上检测完全失效的问题）；创建规则时不再做主机端口占用检查，冲突改由规则上的警告与运行失败呈现
- [ ] CHANGELOG 注明本次需要升级 Agent，升级前相关主机的规则上不会出现占用警告

## 排查文档

- [ ] `docs/guide/troubleshooting.md` 的端口占用章节补一节：规则卡片上的占用警告只对内核转发出现；用户态转发的端口问题表现为规则运行失败并直接点名占用者
