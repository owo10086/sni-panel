# 08 — 修订 SNI 分流文档并完成发布检查

**What to build:** 架构决策、领域词汇、管理员指南和变更记录准确描述入口按域名选择转发链后的最终行为，并在发布前验证入口分流、双端状态和升级提示能够共同工作。

**Blocked by:** 04 — 让共享入口分流表更新只影响变更规则；05 — 以入口和出口共同确认规则运行状态；07 — 在面板升级区域复用入口版本预检；09 — 用 Agent 自己的端口身份撤除共享入口分流器（经由 02）

**Status:** claimed

- [x] ADR-0001 说明分流器同时部署在转发链入口与链路出口，并以入口组会放大限速额度作为出口统一限速、连接数限制和计量的理由
- [x] ADR-0002 说明转发链规则可以跨链路资源共用入口端口、入口运行时采用稳定身份，以及所有相关入口与出口共同决定运行状态
- [x] 领域词汇表区分分流器、入口分流器、出口分流器和两类分流表，并继续把分流表定义为派生物
- [x] 管理员指南删除“入口只负责传输”“仅同一承载资源共用端口”“运行状态只检查出口”等旧行为描述
- [x] 管理员指南补充入口按域名选择转发链、所有相关入口与出口共同确认运行状态、两类未匹配计数、只读全貌页面及版本排查方法
- [x] 管理员指南保留未知域名断开、仅 TCP、管理员专用、出口单点、客户端真实 IP 不保留等既有边界
- [x] 变更记录说明已有入口监听统一切换、升级时发生一次连接中断，以及后续分流表更新保持既有连接
- [x] 文档统一使用“SNI 分流规则”“分流器”“分流表”“转发链”“入口组”和“落地机”等领域词汇
- [x] 发布前验证入口产生的流量数据仍被忽略，出口继续作为唯一计量来源
- [x] SNI 期望状态测试、规则接口测试、前端相关测试、类型检查和生产构建全部通过

## Comments

2026-09-17 实施摘要：核对现有两份架构决策、领域词汇表和变更记录，修订 `docs/guide/sni-routing.md` 与 `docs/guide/rules.md`，补充入口选链、跨链共享、双端状态、两类计数、只读全貌和升级运维。指南构建通过；指南修订与任务摘要均沿用现有文件。

2026-09-17 验证摘要：SNI 专项 `37` 项通过；`pnpm test:server` 的预备测试 `28` 项和主测试 `653` 项全部通过；前端与服务端类型检查、生产构建、指南构建及 `git diff --check` 通过。Agent 的非缓存 Go 全量测试与分流器 SNI Go 专项测试通过。分流器 Go 全量中的两项旧 UDP 测试发送 `32905` 和 `24649` 字节数据报，超过本机 `net.inet.udp.maxdgram=9216`，报出 `message too long`；跳过这两项后，其余既有 Go 测试通过。生产构建保留部分文件超过 `500 kB` 的体积警告。

2026-09-17 审查摘要：以 `git diff bd13d4c` 覆盖 `c83c386` 与未提交修改，编码规范审查发现 `0` 项，规格审查发现入口版本异常时仍下发运行配置的 `P1` 问题。独立浏览器组件验证发现长版本文字横向越界，问题分别记录在任务 02 与任务 07。相关修正、最终文档一致性检查及中文 Git 提交等待确认；真实 Linux 主机首次升级切换与长连接验证仍须部署执行。

2026-09-17 调整确认：同意修正任务 02 的入口停止条件和任务 07 的长版本换行，继续完成最终验证与审查后提交当前分支。出口、端口转发资源及其余 Agent 功能保持原有行为。

2026-09-17 修正后验证摘要：SNI 专项 `37` 项、完整服务器测试的预备 `28` 项与主测试 `653` 项再次全部通过；前后端类型检查、生产构建、指南构建、差异格式检查及版本一致性检查通过。界面预览覆盖桌面、移动端和 `320` 像素窄屏的 `27` 个场景，横向越界、文字重叠和版本内容缺失均为零，临时预览服务结束。指南中入口版本异常停止运行的说明与修正后的配置处理保持一致；最终规范和规格审查继续执行。

2026-09-17 最终审查待确认事项：编码规范审查发现 `0` 项；规格审查新增一项入口停止问题，共享入口删除动作使用服务器当前代表规则编号，而 Agent 本地端口记录可能保存另一条共享规则编号，导致端口保护条件跳过删除。内存 SQLite 心跳验证确认，旧代表规则 `101` 的端口只收到新代表规则 `111` 的删除动作，且端口级动作记录阻止补充旧编号删除。任务 02 恢复为 `claimed`，任务 08 和整体规格继续保持 `claimed`；完善删除身份匹配和相关测试的调整等待确认。现有测试与构建通过结果保留，代码修改与中文 Git 提交暂停。

2026-09-17 最终审查精简摘要：正常稳定心跳返回端口 `18443` 的运行规则 `101, 111, 201, 211, 301, 311`，Agent 保存顺序使本地端口编号成为 `311`；版本异常时服务器只生成编号 `101` 的删除动作，身份保护条件跳过停止。最终结果为编码规范问题 `0` 项、规格问题 `1` 项 `P1`、授权范围之外的新增行为 `0` 项。除该停止条件外，八项任务的现有实现和指南均未发现新增确证问题。诊断使用内存数据库，临时监听正常结束；真实 Linux 首次升级切换、长连接热更新和完整升级操作仍须部署验证。

2026-09-17 继续实施摘要：确认完成任务 02 的最后一项。版本异常删除动作改为优先采用 Agent 上报的入口分流器端口身份，并始终使用 `sni-splitter` 角色；正常共享入口编号 `311` 和删除首条规则后的旧编号 `101` 均通过心跳接口测试。Agent 通用端口保护及本期功能范围保持不变，最终完整测试和双轴审查继续执行。

2026-09-17 拆分：最终审查中的那项 P1 已分离为 09 — 用 Agent 自己的端口身份撤除共享入口分流器。本单的文档修订部分已完成，发布检查待 09 与 02 收口后重跑。

2026-09-17 本轮验证与待确认摘要：服务器预备测试 `28` 项、主测试 `653` 项以及类型检查、生产构建、指南构建和版本检查通过，面板版本保持 `3.2.2`，Agent 与 FXP 保持 `3.2.0`。最终编码规范审查发现 `0` 项；行为审查确认，当前按第一条 SNI 记录生成删除动作仍遗漏真实 Agent 升序上报及没有 `sni` 的旧入口记录，任务 02 的停止运行验收继续待完成。相关证据与按每条本地身份生成撤除的待确认调整记录在任务 02。本单保持 `claimed`，任务 09 的现有内容和状态保留，代码修改、提交及远端发布暂停。

2026-09-17 三项调整验证与审查摘要：逐条本地身份撤除、固定入口分流器专用清理及省略状态类型 rule 的编号 0 动作完成现有心跳验收。服务器预备 28 项、主测试 653 项、SNI 心跳 6 项、前后端类型检查、生产构建、指南构建、版本检查及 Agent 既有专项测试通过。生产构建保留部分文件超过 500 kB 的警告。编码规范审查为 0 项；规格诊断另确认本地编号 0、gost、协议 both 时孤儿清扫重复补发 both 撤除。具体记录去重的补充调整等待确认，任务 02 的最后一项继续待完成，本单保持 claimed，代码修改、中文提交与远端发布暂停。
