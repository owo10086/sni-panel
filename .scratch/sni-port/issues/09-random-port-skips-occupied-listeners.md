# 09 — 随机分配端口避开主机监听

> 本工单已作废；端口占用相关实现以 `.scratch/port-occupancy-rework/spec.md` 和其下的新工单为准。

**What to build:** 在单主机和转发组的规则表单使用随机分配时，系统避开主机上已有的同协议监听端口，使自动选择的端口尽可能无需再次调整。

**Blocked by:** 06 — 转发组与 SNI 分流规则共用端口

**Status:** wontfix

- [ ] 单主机随机分配避开快照中同协议已被外部进程或不可复用受管运行时占用的端口，包括内核转发本可确认使用的端口
- [ ] 转发组随机分配避开任一实际入口主机上已占用的候选端口，并继续遵守主机、套餐和隧道的现有端口范围
- [ ] TCP 与 UDP 分别判断；缺少可靠快照时沿用现有数据库分配方式，页面不把结果保证为主机端口空闲
- [ ] 规则路由测试覆盖单主机、转发组、多协议和快照缺失时的随机端口结果
